check host nodejs with address 0.0.0.0
  start program = "/data/<%= @app_name %>/current/vendor/node/njs start"
  stop program = "/data/<%= @app_name %>/current/vendor/node/njs stop"
  if failed port 80 protocol HTTP
    request /
    with timeout 10 seconds
    then restart